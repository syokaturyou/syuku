<div class="container">
<div class="row">
  <div class="col-3">
   <%= render "users/shared/bubun" %>
  </div>

<div class="cccc">
 <div class="aaaa">
 <h3>プロフィール</h3>
  <table class="acounttable">
   <tbody>
    <tr>
     <td></td>
     <td><%= attachment_image_tag @user, :profileimage, fallback: "no_image_logo.png", size: '80x80', class: "aicon" %></td>
    </tr>
    <%= form_with model:@user, url: user_path(@user), local: true do |f| %>
     <tr>
      <th><%= f.label :アイコン画像 %></th>
      <td>
        <label><%= check_box_tag :image_delete %>画像なし</label>
        <label><%= f.attachment_field :profileimage %></label>
      </td>
     </tr>
    <tr>
     <th><%= f.label :名前 %></th>
     <td><%= f.text_field :name, autofocus: true, required: true, maxlength: "40", size: "40" %></td>
    </tr>
    <tr>
     <th><%= f.label :自己紹介 %></th>
     <td><%= f.text_area :profile, autofocus: true,  size: '40x5' %></td>
    </tr>
    <tr>
     <td><%= f.submit "更新する", class: 'btn btn-primary' %></td>
    <% end %>
    </tr>
   </tbody>
  </table>
 </div>
</div>
 
</div>
</div>