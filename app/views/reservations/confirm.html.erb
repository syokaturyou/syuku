 <h2>予約確認</h2>
 
 <table class="acounttable">
   <tbody>
     一日辺り金額
     
    <%= form_with(model: @reservation, local: true) do |f| %>
    <tr>
     <td>開始日<%= f.hidden_field :startdate, :value => @reservation.startdate %></td>
    </tr>
    <tr>
     <td><%= @reservation.startdate %></td>
    </tr>
    <tr>
     <td>終了日<%= f.hidden_field :enddate, :value => @reservation.enddate %></td>
    </tr>
    <tr>
     <td><%= @reservation.enddate %></td>
    </tr>
    <%= f.hidden_field :user_id, :value => current_user.id %>
    <%= f.hidden_field :room_id, :value => @reservation.room_id %>
    <%= f.hidden_field :totalprice, :value => @reservation.totalprice %>
    <tr>
     <td>□使用日数:<%= (@reservation.enddate - @reservation.startdate).to_int %>日</td>
    </tr>
    <tr>
    <td><%= f.hidden_field :human, :value => @reservation.human %>□人数:<%= @reservation.human %>人</td>
    </tr>
    <tr>
     <td>合計金額</td>
    </tr>
    <tr>
     <td>
      <%= (@reservation.enddate - @reservation.startdate).to_int * @reservation.human %></td>
    </tr>
    <tr>
     <td>上記内容でよろしければ「予約を確定」を押してください</td>
    </tr>
    <tr>
     <td><%= f.submit "予約を確定", class: 'btn btn-primary' %></td>
    </tr>
    <tr>
     <td><%= link_to 'ルーム詳細に戻る', rooms_path, class: "btn btn-outline-primary" %></td>
    </tr>
    <% end %>
   </tbody>
  </table>